<!DOCTYPE html>
<html>
    <head>
        <title>Log Page</title>
        <link rel="stylesheet" href="{{ url_for('static', filename='log.css')}}">
    </head>
    <body>
        <h1>Logs</h1>
        <p>Logs generated in response to requests.</p>
        <h2>Specification Warnings</h2>
        <dl>
            {% for timestamp, endpoint, method, warning in logs %}
                <dt>At {{ timestamp }}: {{ method }} {{ endpoint }}</dt>
                <dd>{{ warning|e }}</dd>
            {% endfor %}
        </dl>
    </body>
</html>