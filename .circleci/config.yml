version: 2.1
jobs:
  build:
    docker:
      - image: google/cloud-sdk:latest
    steps:
      - checkout
      - run: gcloud app deploy --project petfinder-424117

workflows:
  build_app:
    jobs:
      - build:
          filters:
            branches:
              only:
                - master

