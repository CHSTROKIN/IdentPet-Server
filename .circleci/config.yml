version: 2.1

jobs:
  build:
    steps:
      - checkout
      - run:
          name: Build the app
          command: gcloud app deploy

workflows:
  build_app:
    jobs:
      - build:
          filters:
            branches:
              only: master
